## Why

The hello world page already shows "Hello, World!" The user wants a minimal edit: keep that text and append "Shashi" at the end of the heading in `index.html`.

## What Changes

- Modify existing `index.html` `<h1>` from `Hello, World!` to `Hello, World! Shashi` (append name at the end)
- Update README expected output to match
- Leave page title and CSS unchanged unless needed for consistency

## Capabilities

### New Capabilities

<!-- None -->

### Modified Capabilities

- `hello-world-page`: Greeting SHALL retain "Hello, World!" and append "Shashi" at the end of the heading text

## Impact

- `index.html` — `<h1>` text only
- `README.md` — expected output description
