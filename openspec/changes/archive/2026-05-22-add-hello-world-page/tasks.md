## 1. Static page files

- [x] 1.1 Create `index.html` with `lang="en"`, viewport meta, document title, and `<h1>Hello, World!</h1>`
- [x] 1.2 Create `styles.css` with centered layout and readable typography; link from `index.html`

## 2. Verification and docs

- [x] 2.1 Open `index.html` in a browser and confirm the greeting displays correctly
- [x] 2.2 Add a short README section with steps to view the page locally (open file or optional static server command)
